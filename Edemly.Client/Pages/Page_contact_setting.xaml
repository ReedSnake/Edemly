<Page x:Class="Edemly.Client.Page_contact_setting"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Edemly.Client"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1280"
      Title="Contact Settings"
      Background="#057272">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                <GradientStop Color="#004040" Offset="0.7"/>
                <GradientStop Color="#057272" Offset="0.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="#F6FFFC"
                CornerRadius="40" 
                Margin="35,36,0,34">
            <ScrollViewer VerticalScrollBarVisibility="Hidden" 
                          HorizontalScrollBarVisibility="Disabled" 
                          Margin="18,6,4,0">
                <Grid Height="598">
                    <Grid Margin="20,20,20,0" VerticalAlignment="Top">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="BackButton"
                        Grid.Column="0"
                        Click="BackButton_Click"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Background="Transparent"
                        Foreground="{DynamicResource ThemeBackgroundDarkBrush}"
                        FontSize="24"
                        FontWeight="Bold"
                        Width="40"
                        Height="40"
                        BorderThickness="0"
                        BorderBrush="Transparent"
                        Cursor="Hand"
                        Panel.ZIndex="1000"
                        Content="←">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="CornerRadius" Value="8"/>
                                                        <Setter TargetName="border" Property="Background" Value="#E6F3F3"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <StackPanel Margin="0,70,0,20" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Top">
                        <Border Width="120" Height="120" 
                                CornerRadius="60" 
                                Margin="0,0,0,20" 
                                HorizontalAlignment="Center"
                                BorderBrush="#CCCCCC"
                                BorderThickness="2"
                                ClipToBounds="True">
                            <Border.Background>
                                <ImageBrush x:Name="ContactPhotoBackground" 
                                           Stretch="UniformToFill"/>
                            </Border.Background>
                        </Border>

                        <TextBlock x:Name="NameTextBox"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource ThemeSecondaryBrush}"
                                   TextAlignment="Center"
                                   Margin="0,0,0,20"/>

                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Center" 
                                    Margin="0,0,0,10">
                            <TextBlock Text="📞" 
                                       FontSize="18" 
                                       Margin="0,0,8,0"/>
                            <TextBlock x:Name="PhoneTextBox"
                                       FontSize="16"
                                       Foreground="#222"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Center" 
                                    Margin="0,0,0,30">
                            <TextBlock Text="✉️" 
                                       FontSize="18" 
                                       Margin="0,0,8,0"/>
                            <TextBlock x:Name="EmailTextBox"
                                       FontSize="16"
                                       Foreground="#222"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Notes Section -->
                        <StackPanel Margin="20,0,20,20">
                            <TextBlock x:Name="NotesSectionTitle"
                                       Text="Note"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ThemeSecondaryBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBlock x:Name="NotesPrivateText"
                                       Text="*only you can see this information"
                                       FontSize="12"
                                       Foreground="#CDCDCD"
                                       Margin="0,2,0,15"/>

                            <StackPanel x:Name="NotesPanel" Margin="0,0,0,10"/>
                            <Border x:Name="AddNoteSection" 
                            Background="{DynamicResource ThemeBorderLightBrush}"                                    
                                    CornerRadius="10"
                                    Padding="12"
                                    Height="162">
                                <StackPanel>
                                    <TextBox x:Name="NewNoteTextBox"
                                     FontSize="13"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     MaxLines="5" 
                                     BorderThickness="1"
                                     BorderBrush="#0B4539"
                                     Padding="8"
                                     Margin="0,0,0,8" 
                                     Width="408" />

                                    <StackPanel Orientation="Horizontal" 
                                                HorizontalAlignment="Right">
                                        <Button x:Name="DeleteNoteButton"
                                                Content="Delete"
                                                Click="DeleteNoteButton_Click"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Padding="15,8"
                                                FontSize="13"
                                                FontWeight="SemiBold"
                                                Cursor="Hand"
                                                Margin="0,0,8,0"
                                                Visibility="Collapsed">
                                            <Button.Style>
                                                <Style TargetType="{x:Type Button}">
                                                    <Setter Property="Background" Value="#FF5252"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type Button}">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="8"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                                      VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#D32F2F"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Button x:Name="AddNoteButton"
                                                Content="Add Note"
                                                Click="AddNoteButton_Click"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Padding="15,8"
                                                FontSize="13"
                                                FontWeight="SemiBold"
                                                Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="{x:Type Button}">
                                                    <Setter Property="Background" Value="#0B4539"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type Button}">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="8"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                                      VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#0D47A1"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <TextBlock x:Name="MaxNotesWarning"
                                       Text="Maximum 5 contacts with notes reached"
                                       Foreground="#FF5252"
                                       FontSize="12"
                                       Margin="0,10,0,0"
                                       Visibility="Collapsed"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Border>

        <Border Grid.Column="2"
                CornerRadius="40" 
                ClipToBounds="True"
                Background="Transparent"
                Margin="0,27,10,23">
            <Grid>
                <Image Source="/Assets/image_1.png" 
                       Stretch="Fill" Margin="-9,-4,6,0"/>
            </Grid>
        </Border>
    </Grid>
</Page>